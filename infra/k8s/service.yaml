apiVersion: v1
kind: Service
metadata:
  name: moraai-platform
  namespace: moraai
  labels:
    app: moraai-platform
spec:
  type: LoadBalancer
  selector:
    app: moraai-platform
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
